server:
  port: 8080
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/demo1?useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:cn/zhen77/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    cn:
      zhen77:
        mapper: debug